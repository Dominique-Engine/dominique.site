query BlogPosts {
    pages(where: {type: BlogPost}, orderBy: createdAt_DESC) {
        id
        title
        slug
        meta {
            image {
                url
            }
            description
        }
        createdAt
    }
}
